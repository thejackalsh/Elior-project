# Salin ke .env.local:  cp .env.example .env.local
#
# Hanya dipakai saat `npm run dev`. Untuk Docker, nilai diambil dari .env di
# root repo dan dibakar saat build (lihat docker-compose.yml).

NEXT_PUBLIC_BACKEND_URL=http://localhost:8080
NEXT_PUBLIC_API_KEY=
